import java.net.DatagramSocket;
import java.net.InetAddress;
import java.net.SocketException;
import java.util.ArrayList;

public class Broadcaster implements Runnable {

	private ArrayList<InetAddress> destinations; 
	private String message;
	private int port;
	private DatagramSocket broadcaster;
	
	public Broadcaster(int port) throws SocketException {
		this.destinations = new ArrayList<InetAddress>();
		this.port = port;
		broadcaster = new DatagramSocket(port);
	}
	
	public void setDestinations(ArrayList<InetAddress> destinations) {
		this.destinations = destinations;
	}
	
	@Override
	public void run() {
		// TODO Auto-generated method stub

	}

}
